apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: korus-leader-election-role
  namespace: korus-system
rules:
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - create
  - get
  - list
  - update
  - watch
