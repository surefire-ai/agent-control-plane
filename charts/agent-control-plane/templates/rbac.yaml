{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ printf "%s-manager-role" (include "agent-control-plane.fullname" .) | trunc 63 | trimSuffix "-" | quote }}
  labels:
    {{- include "agent-control-plane.labels" . | nindent 4 }}
rules:
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - pods/log
  verbs:
  - get
- apiGroups:
  - batch
  resources:
  - jobs
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - windosx.com
  resources:
  - agentpolicies
  - agents
  - knowledgebases
  - mcpservers
  - prompttemplates
  - toolproviders
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - windosx.com
  resources:
  - agentruns
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - windosx.com
  resources:
  - agentruns/status
  - agents/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ printf "%s-manager-rolebinding" (include "agent-control-plane.fullname" .) | trunc 63 | trimSuffix "-" | quote }}
  labels:
    {{- include "agent-control-plane.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ printf "%s-manager-role" (include "agent-control-plane.fullname" .) | trunc 63 | trimSuffix "-" | quote }}
subjects:
- kind: ServiceAccount
  name: {{ include "agent-control-plane.serviceAccountName" . | quote }}
  namespace: {{ .Release.Namespace | quote }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ printf "%s-leader-election-role" (include "agent-control-plane.fullname" .) | trunc 63 | trimSuffix "-" | quote }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "agent-control-plane.labels" . | nindent 4 }}
rules:
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - create
  - get
  - list
  - update
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ printf "%s-leader-election-rolebinding" (include "agent-control-plane.fullname" .) | trunc 63 | trimSuffix "-" | quote }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "agent-control-plane.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ printf "%s-leader-election-role" (include "agent-control-plane.fullname" .) | trunc 63 | trimSuffix "-" | quote }}
subjects:
- kind: ServiceAccount
  name: {{ include "agent-control-plane.serviceAccountName" . | quote }}
  namespace: {{ .Release.Namespace | quote }}
{{- end }}
