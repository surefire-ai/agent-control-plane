apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: agent-control-plane-leader-election-role
  namespace: agent-control-plane-system
rules:
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - create
  - get
  - list
  - update
  - watch
